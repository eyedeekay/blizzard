name: Go

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.21.x' ]

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      # You can test your matrix by printing the current Go version
      - name: Display Go version
        run: go version
      - name: Build executables
        run: go build -o snowflake-linux
      - name: Upload 
        uses: actions/upload-artifact@v4
        with:
          path: snowflake-linux
          name: snowflake-linux
      - name: Upload artifacts
        uses: ncipollo/release-action@v1
        with:
          artifacts: snowflake-linux
          prerelease: true
          allowUpdates: true
          replacesArtifacts: true
          makeLatest: true
          tag: nightly
          bodyFile: "README.md"


  buildmac:

    runs-on: macos-latest
    strategy:
      matrix:
        go-version: [ '1.21.x' ]

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      # You can test your matrix by printing the current Go version
      - name: Display Go version
        run: go version
      - name: Build executables
        run: go build -o snowflake-macos
      - name: Upload 
        uses: actions/upload-artifact@v4
        with:
          path: snowflake-macos
          name: snowflake-macos
      - name: Upload artifacts
        uses: ncipollo/release-action@v1
        with:
          artifacts: snowflake-macos
          prerelease: true
          allowUpdates: true
          replacesArtifacts: true
          makeLatest: true
          tag: nightly
          bodyFile: "README.md"


  buildwin:

    runs-on: windows-latest
    strategy:
      matrix:
        go-version: [ '1.21.x' ]

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      # You can test your matrix by printing the current Go version
      - name: Display Go version
        run: go version
      - name: Build executables
        run: go build -o snowflake-windows.exe
      - name: Upload 
        uses: actions/upload-artifact@v4
        with:
          path: snowflake-windows.exe
          name: snowflake-windows.exe
      - name: Upload artifacts
        uses: ncipollo/release-action@v1
        with:
          artifacts: snowflake-windows.exe
          prerelease: true
          allowUpdates: true
          replacesArtifacts: true
          makeLatest: true
          tag: nightly
          bodyFile: "README.md"
